apiVersion: v1
kind: Service
metadata:
  name: {{ include "exampleapp-api.fullname" . }}
  labels:
    {{- include "exampleapp-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exampleapp-api.selectorLabels" . | nindent 4 }}
